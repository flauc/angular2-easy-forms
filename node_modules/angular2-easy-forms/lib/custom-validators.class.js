"use strict";
var CustomValidators = (function () {
    function CustomValidators() {
    }
    CustomValidators.match = function (key) {
        return function (control) {
            if (control.value && control.root.controls)
                return control.root.controls[key].value !== control.value ? { 'match': { 'currentValue': control.value, 'requiredValue': control.root.controls[key].value } } : null;
            return null;
        };
    };
    return CustomValidators;
}());
exports.CustomValidators = CustomValidators;
//# sourceMappingURL=custom-validators.class.js.map